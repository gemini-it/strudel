---
title: Premières Notes
layout: ../../../layouts/MainLayout.astro
---

import { MiniRepl } from '@src/docs/MiniRepl';
import { midi2note } from '@strudel/core/';
import Box from '@components/Box.astro';
import QA from '@components/QA';

# Premières Notes

Maintenant, nous allons voir comment jouer avec des notes en utilisant la fonction `note`.

## Chiffres et Notes

**Jouer des notes avec des nombres**

<MiniRepl
  client:visible
  tune={`note("48 52 55 59").sound("piano")`}
  claviature
  claviatureLabels={Object.fromEntries(
    Array(49)
      .fill()
      .map((_, i) => [midi2note(i + 36), i + 36]),
  )}
/>

<Box>

Essaie différents chiffres !

Essaie aussi des nombres décimaux, par exemple 55.5 (note la façon anglaise d'écrire les nombres décimaux avec un "." au lieu d'une virgule).

</Box>

**Jouer des notes avec des lettres**

<MiniRepl
  client:visible
  tune={`note("c e g b").sound("piano")`}
  claviature
  claviatureLabels={Object.fromEntries(['c3', 'd3', 'e3', 'f3', 'g3', 'a3', 'b3'].map((n) => [n, n.split('')[0]]))}
/>

<Box>

Essaie différentes lettres (a - g).

Trouves-tu des mélodies qui sont aussi un mot ? Indice : ☕ 🙈 🧚

</Box>

**Altérations**

<MiniRepl
  client:visible
  tune={`note("db eb gb ab bb").sound("piano")`}
  claviature
  claviatureLabels={Object.fromEntries(
    ['db3', 'eb3', 'gb3', 'ab3', 'bb3'].map((n) => [n, n.split('').slice(0, 2).join('')]),
  )}
/>

<MiniRepl
  client:visible
  tune={`note("c# d# f# g# a#").sound("piano")`}
  claviature
  claviatureLabels={Object.fromEntries(
    ['c#3', 'd#3', 'f#3', 'g#3', 'a#3'].map((n) => [n, n.split('').slice(0, 2).join('')]),
  )}
/>

**Autres octaves**

<MiniRepl
  client:visible
  tune={`note("c2 e3 g4 b5").sound("piano")`}
  claviature
  claviatureLabels={Object.fromEntries(['c1', 'c2', 'c3', 'c4', 'c5'].map((n) => [n, n]))}
  claviatureLabels={Object.fromEntries(
    Array(49)
      .fill()
      .map((_, i) => [midi2note(i + 36), midi2note(i + 36)]),
  )}
/>

<Box>

Essaie différentes octaves (1-8)

</Box>

Normalement, les personnes qui ne lisent pas de partitions s'en sortent mieux avec les chiffres qu'avec les lettres.
Par conséquent, les exemples suivants utiliseront principalement des chiffres.
Plus tard, nous verrons aussi quelques astuces pour nous aider à jouer des notes qui s'accordent bien.

## Changer le son

Tout comme pour les sons bruités, nous pouvons modifier le son de nos notes avec `sound` :

<MiniRepl client:visible tune={`note("36 43, 52 59 62 64").sound("piano")`} />

<Box>

Essaie quelques sons :

- gm_electric_guitar_muted - Guitare électrique
- gm_acoustic_bass - Contrebasse
- gm_voice_oohs - Accords
- gm_blown_bottle - Bouteille
- sawtooth - Onde en dents de scie
- square - Onde carrée
- triangle - Onde triangulaire
- Qu'en est-il de bd, sd ou hh ?
- Supprime `.sound('...')` complètement

</Box>

**Alterner entre les sons**

<MiniRepl
  client:visible
  tune={`note("48 67 63 [62, 58]")
.sound("piano gm_electric_guitar_muted")`}
/>

**Sons simultanés**

<MiniRepl
  client:visible
  tune={`note("48 67 63 [62, 58]")
.sound("piano, gm_electric_guitar_muted")`}
/>

<Box>

Les motifs dans `note` et `sound` sont combinés !

Nous verrons plus tard d'autres façons de combiner les motifs.

</Box>

## Séquences plus longues

**Ralentir les séquences avec `/`**

<MiniRepl client:visible tune={`note("[36 34 41 39]/4").sound("gm_acoustic_bass")`} punchcard />

<Box>

Le `/4` joue la séquence 4 fois plus lentement, soit un total de 4 cycles = 8 secondes.

Chaque note dure donc maintenant 2 secondes.

Écris plus de notes entre parenthèses et observe comment ça va plus vite.

</Box>

Si une séquence doit toujours conserver la même vitesse, quel que soit son contenu, il existe un autre type de parenthèses :

**Un par cycle avec \< \>**

Dans le dernier chapitre, nous avons déjà appris que `< ... >` (chevrons) ne jouent qu'un seul élément par cycle.
C'est également très utile pour les mélodies :

<MiniRepl client:visible tune={`note("<36 34 41 39>").sound("gm_acoustic_bass")`} punchcard />

<Box>

Ajoute d'autres notes et observe comment le tempo reste le même !

En fait, ces parenthèses ne sont qu'une abréviation :

`<a b c>` = `[a b c]/3`

`<a b c d>` = `[a b c d]/4`

etc..

</Box>

**Une séquence par cycle**

<MiniRepl
  client:visible
  tune={`note("<[36 48] [34 46] [41 53] [39 51]>")
.sound("gm_acoustic_bass")`}
  punchcard
/>

ou aussi...

<MiniRepl
  client:visible
  tune={`note("<[36 48]*4 [34 46]*4 [41 53]*4 [39 51]*4>/2")
.sound("gm_acoustic_bass")`}
  punchcard
/>

**Alternatives**

Comme pour les sous-séquences, `<...>` peut également être utilisé dans une séquence :

<MiniRepl
  client:visible
  tune={`note("60 <63 62 65 63>")
.sound("gm_xylophone")`}
  punchcard
/>

C'est aussi pratique pour les sons atonaux :

<MiniRepl
  client:visible
  tune={`sound("bd*2, ~ <sd cp>, [~ hh]*2")
.bank("RolandTR909")`}
  punchcard
/>

## Échelles

Il peut être fastidieux de trouver les bonnes notes quand on a toutes les options disponibles.
Avec les échelles, c'est plus simple :

<MiniRepl
  client:visible
  tune={`n("0 2 4 <[6,8] [7,9]>")
.scale("C:minor").sound("piano")`}
  punchcard
/>

<Box>

Essaie différents chiffres. Chacun sonne bien !

Essaie différentes échelles :

- C:major
- A2:minor
- D:dorian
- G:mixolydian
- A2:minor:pentatonic
- F:major:pentatonic

</Box>

**Échelles automatisées**

Comme toutes les fonctions, les échelles peuvent également être automatisées avec un motif :

<MiniRepl
  client:visible
  tune={`n("<0 -3>, 2 4 <[6,8] [7,9]>")
.scale("<C:major D:mixolydian>/4")
.sound("piano")`}
  punchcard
/>

<Box>

Si tu n'as pas la moindre idée de ce que signifient les échelles, ne t'inquiète pas.
Ce sont simplement des noms pour différents groupes de sons qui s'accordent bien.

Prends le temps de découvrir quelles échelles tu aimes.

</Box>

## Répéter et Étendre

**Étendre avec @**

<MiniRepl client:visible tune={`note("c@3 eb").sound("gm_acoustic_bass")`} punchcard />

<Box>

Un élément sans `@` est équivalent à `@1`. Dans l'exemple, `c` a une longueur de trois unités, `eb` seulement une.

Joue avec la longueur !

</Box>

**Étendre les sous-séquences**

<MiniRepl
  client:visible
  tune={`n("<[4@2 4] [5@2 5] [6@2 6] [5@2 5]>*2")
.scale("<C2:mixolydian F2:mixolydian>/4")
.sound("gm_acoustic_bass")`}
  punchcard
/>

<Box>

Ce rythme est également appelé `shuffle`.
Chaque temps contient 2 notes, la première étant deux fois plus longue que la seconde.
Cela est parfois appelé `swing ternaire`. C'est un rythme typique du blues et du jazz.

</Box>

**Répéter**

<MiniRepl client:visible tune={`note("c!2 [eb,<g a bb a>]").sound("piano")`} punchcard />

<Box>

Alternez entre `!`, `*` et `@`.

Quelle est la différence ?

</Box>

## Résumé

Voici ce que nous avons appris dans ce chapitre :

| Concept      | Syntax | Example                                                  |
| ------------ | ------ | -------------------------------------------------------- |
| Ralentir     | \/     | <MiniRepl client:visible tune={`note("[c a f e]/2")`} /> |
| Alternatives | \<\>   | <MiniRepl client:visible tune={`note("c <e g>")`} />     |
| Étendre      | @      | <MiniRepl client:visible tune={`note("c@3 e")`} />       |
| Répéter      | !      | <MiniRepl client:visible tune={`note("c!3 e")`} />       |

Nouvelles fonctions :

| Name  | Description                             | Example                                                                           |
| ----- | --------------------------------------- | --------------------------------------------------------------------------------- |
| note  | Hauteur en tant que lettre ou nombre      | <MiniRepl client:visible tune={`note("b g e c").sound("piano")`} />               |
| scale | Interprète `n` comme un degré d'échelle   | <MiniRepl client:visible tune={`n("6 4 2 0").scale("C:minor").sound("piano")`} /> |
| stack | Joue plusieurs motifs en parallèle (voir ci-dessous) | <MiniRepl client:visible tune={`stack(s("bd sd"),note("c eb g"))`} />             |

## Exemples

**Ligne de basse**

<MiniRepl
  client:visible
  tune={`note("<[c2 c3]*4 [bb1 bb2]*4 [f2 f3]*4 [eb2 eb3]*4>/2")
.sound("gm_synth_bass_1")
.lpf(800) // <-- we'll learn about this soon`}
/>

**Mélodie**

<MiniRepl
  client:visible
  tune={`n(\`<
[~ 0] 2 [0 2] [~ 2]
[~ 0] 1 [0 1] [~ 1]
[~ 0] 3 [0 3] [~ 3]
[~ 0] 2 [0 2] [~ 2]
>*2\`).scale("C4:minor")
.sound("gm_synth_strings_1")`}
/>

**Batterie**

<MiniRepl
  client:visible
  tune={`sound("bd*2, ~ <sd cp>, [~ hh]*2")
.bank("RolandTR909")`}
/>

**S'il y avait un moyen de tout jouer en même temps.......**

<Box>

C'est possible avec `stack` 😙

</Box>

<MiniRepl
  client:visible
  tune={`stack(
  note("<[c2 c3]*4 [bb1 bb2]*4 [f2 f3]*4 [eb2 eb3]*4>/2")
  .sound("gm_synth_bass_1").lpf(800),
  n(\`<
  [~ 0] 2 [0 2] [~ 2]
  [~ 0] 1 [0 1] [~ 1]
  [~ 0] 3 [0 3] [~ 3]
  [~ 0] 2 [0 2] [~ 2]
  >*2\`).scale("C4:minor")
  .sound("gm_synth_strings_1"),
  sound("bd*2, ~ <sd cp>, [~ hh]*2")
  .bank("RolandTR909")
)`}
/>

Ça ressemble enfin à de la vraie musique !
Nous avons les sons, nous avons les notes... il manque encore une pièce du puzzle : les [Effets](/de/workshop/first-effects).
